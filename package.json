{
  "name": "recetario-comidas",
  "version": "0.1.0",
  "private": true,
  "description": "Recetario visual con busqueda por ingredientes y recomendaciones de despensa.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "db:migrate": "node src/db/migrate.js",
    "check": "node --check src/server.js && node --check src/db/migrate.js && node --check src/services/recipes.js && node --check src/services/recipeVision.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
